name: Tests On Pull Request

on:
  pull_request:
    branches: [ dev ]

jobs:
  run-tests-and-upload-coverage:
    uses: dsk1306/combineExtensions/.github/workflows/run-tests-and-upload-coverage.yml@codecov
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
